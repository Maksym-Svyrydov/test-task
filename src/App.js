import { AppBar } from './components/Header/Header';
import { Layout } from './components/Layout/Layout';

export const App = () => {
  return (
    <Layout>
      <AppBar></AppBar>
    </Layout>
  );
};

export default App;
